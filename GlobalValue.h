int ATYPE[MODNUM_MAX];							//VM type assigned to each module
int CP[MODNUM_MAX];								//the module on CP
unordered_map<int, vector<int>> preNodes;       //previous nodes of each node
//unordered_map<int, vector<int>> postNodes;      //the post nodes of each node
